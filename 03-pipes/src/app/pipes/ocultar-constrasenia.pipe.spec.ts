import { OcultarConstraseniaPipe } from './ocultar-constrasenia.pipe';

describe('OcultarConstraseniaPipe', () => {
  it('create an instance', () => {
    const pipe = new OcultarConstraseniaPipe();
    expect(pipe).toBeTruthy();
  });
});
